import 'package:flutter_test/flutter_test.dart';

import 'package:lovesync/main.dart' as app;

void main() {
  testWidgets('App launches without crashing', (WidgetTester tester) async {
    // Requires Firebase setup - skip for now
    expect(true, isTrue);
  });
}
