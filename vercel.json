{
  "installCommand": "",
  "buildCommand": "git clone --depth 1 https://github.com/flutter/flutter.git -b stable flutter_sdk && flutter_sdk/bin/flutter config --enable-web && flutter_sdk/bin/flutter precache --web && flutter_sdk/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "routes": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
